<%= link_to 'Home', apartments_path %> |

<% if current_owner.nil? %>
<%= link_to 'Register', new_owner_registration_path %> |
<%= link_to 'Sign In', new_owner_session_path %> |
<%= link_to "Sign in with Twitter", owner_twitter_omniauth_authorize_path %>
<% end %>

<% if !current_owner.nil? %>

  <%= link_to 'Sign Out', destroy_owner_session_path, method: 'delete' %>

  <% current_owner.roles.each do |role| %> |
    <%= role.name %>
  <%end%>

<% end %>


<% if !current_owner.nil? && current_owner.has_role?("admin") %>
  | <%= link_to "Admin Page", admin_path %>
<% end %>
